public class ex066 {
    public static void main(String[] args) {

        // 함수(메소드) Function(Method)

        // 함수의 선언부
        /*
        반환형 함수이름(매개변수타입 매개변수이름){
            실행문;
            return; or return 반환값;
         }
        */

        // 함수의 실행부(호출)
        // 함수이름(매개변수값);

    }
}
